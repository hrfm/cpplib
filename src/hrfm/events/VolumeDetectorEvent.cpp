#include "VolumeDetectorEvent.h"

namespace hrfm { namespace events{
    
    const string VolumeDetectorEvent::DETECTED       = "detected";
    const string VolumeDetectorEvent::BEFORE_UNMOUNT = "before_unmount";
    const string VolumeDetectorEvent::UNMOUNTED      = "unmount";
    
}}

